# Objections — Revision

The objections log has been simplified. The following objections were never raised by Northwind and should be removed from the record:

- **Latency SLA vs self-managed Kafka** — never raised. Northwind has no latency SLA requirement.
- **Data residency** — never raised. Northwind has no residency requirement.
- **Active-active multi-region failover** — never raised. Northwind has no resilience requirement.

The only objection on file is that Northwind would like a discount on the proposed pricing.

## Status

All objections except pricing are closed. Pricing is the only open item.
